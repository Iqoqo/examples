version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2
    steps:
      - checkout # check out the code in the project directory
      - run: echo "a better test is coming i promise"
